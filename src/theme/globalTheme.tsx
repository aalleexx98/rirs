import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    globalMargin: {
        margin: 20,
    },
    titlePrincipal: {
        fontSize: 35,
        fontWeight: '600',
    }
});